---
dependencies:
  - role: secrets
  - role: alloy
    # Metrics only: testnodes are out of scope for the LokiStack.
    alloy_logs_enabled: false
    when: run_alloy_role is defined and run_alloy_role
